% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msvar_engine.R
\name{fit_var}
\alias{fit_var}
\title{Fit a least-squares vector autoregression VAR(p).}
\usage{
fit_var(y, p, subset = NULL)
}
\arguments{
\item{y}{Numeric matrix (\verb{samples x channels}); a vector is treated as a
single channel.}

\item{p}{Autoregressive order; a positive integer with \code{p < nrow(y)}.}

\item{subset}{Optional integer index of rows to use as regression targets;
defaults to all rows after the first \code{p}.}
}
\value{
List with \code{theta} (\code{mu} length-\code{n}, \code{A} \verb{n x (n*p)} stacked
coefficients, \code{Q} \verb{n x n} innovation covariance), the log-likelihood \code{LL},
and pseudo-\code{R2}; with attributes \code{model = "var"} and \code{length = nrow(y)}.
Returns \code{NULL} when the usable target set is empty.
}
\description{
Ordinary-least-squares VAR(p) estimate of the stacked coefficient matrix,
innovation covariance, and mean of a multivariate series, with a
pseudo-inverse fallback for rank-deficient designs and an eigenvalue floor on
the innovation covariance.
}
\examples{
set.seed(1)
y <- matrix(rnorm(500 * 2), 500, 2)
fit <- fit_var(y, p = 1)
fit$theta$A
}
\references{
D. Degras, C.-M. Ting, H. Ombao (2022). Markov-switching
state-space models with applications to neuroimaging. CSDA.
}
