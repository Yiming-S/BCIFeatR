% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandpower.R
\name{extract_bandpower}
\alias{extract_bandpower}
\title{Extract per-channel band-power features over a frequency-band bank.}
\usage{
extract_bandpower(
  data,
  fs,
  frequency_bands,
  order = 3,
  relative = FALSE,
  filters = NULL
)
}
\arguments{
\item{data}{List of trial matrices (\verb{samples x channels}).}

\item{fs}{Sampling rate in Hz.}

\item{frequency_bands}{Non-empty list of \code{c(low, high)} bands in Hz.}

\item{order}{Butterworth filter order.}

\item{relative}{When \code{TRUE}, divide each channel's per-band powers by their
sum across the bank (per trial) and return the fractions in \verb{(0, 1)};
otherwise return log band power.}

\item{filters}{Optional pre-built filter bank from \code{build_bandpass_bank()};
when supplied, \code{fs} and \code{order} are ignored. Use this to avoid redesigning
identical filters once per trial.}
}
\value{
Numeric matrix (\verb{n_trials x (n_bands * n_channels)}) in band-major
order: all channels of band 1, then band 2, and so on.
}
\description{
Each band is band-pass filtered and the per-channel variance is taken as a
band-power estimate (Parseval's relation). The log band power is returned by
default, or the within-channel relative band power (fraction of total power
across the bank) when \code{relative = TRUE}.
}
