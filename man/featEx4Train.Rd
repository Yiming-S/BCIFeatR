% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FeatureExtraction.R, R/aliases.R
\name{featEx4Train}
\alias{featEx4Train}
\alias{feat_ex_train}
\title{Train feature extractors and compute feature matrix.}
\usage{
featEx4Train(
  x,
  y,
  feature,
  params = list(),
  epsilon = 1e-06,
  simplify = TRUE,
  skip_validation = FALSE
)

feat_ex_train(
  x,
  y,
  feature,
  params = list(),
  epsilon = 1e-06,
  simplify = TRUE,
  skip_validation = FALSE
)
}
\arguments{
\item{x}{Trial list or session list.}

\item{y}{Labels aligned with \code{x}; can be NULL only for ATM.}

\item{feature}{One of logvar/logvar_pca/CSP/FBCSP/FBCSSP/TS/ACM_TS/Riemannian/ATM.}

\item{params}{Feature-specific parameter list. For SPD-based features (\code{TS},
\code{ACM_TS}, \code{Riemannian}) the optional field \code{params$metric} selects the
tangent-space reference metric (\code{"euclid"} default, \code{"logeuclid"},
\code{"riemann"}). For \code{TS}/\code{Riemannian} an optional \code{params$cov_list} of
pre-computed per-trial covariance matrices bypasses recomputation and is
useful when the same trial list is reused across CV folds.}

\item{epsilon}{Numerical floor used in SPD operations.}

\item{simplify}{Whether to unwrap a single-session return to a single object.}

\item{skip_validation}{When \code{TRUE}, bypasses the per-trial finiteness and
label-contract checks. Intended for tight CV loops where callers have
already validated inputs upstream; never set this on untrusted data.}
}
\value{
List with \code{features} matrix and \code{object}; or list of such results for sessions.
}
\description{
Supports both trial-list input (\code{x} is list of matrices) and session-list input
(\code{x} is list of trial lists). Returns train-time state required to reproduce the
same transform in \code{featEx4Test}.
}
