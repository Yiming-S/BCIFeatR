% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcca.R
\name{mcca_train}
\alias{mcca_train}
\title{Train a regularized SUMCOR multiset CCA (MCCA) over feature views.}
\usage{
mcca_train(views, ncomp = 1L, reg = 0.001, center = TRUE, scale = TRUE)
}
\arguments{
\item{views}{List of numeric matrices, one per view, each
\verb{n_trials x d_view}, all sharing the same \code{n_trials}.}

\item{ncomp}{Number of canonical components to keep.}

\item{reg}{Ridge added to each within-view block, relative to its mean
diagonal, for numerical stability.}

\item{center, scale}{Whether to center/scale each view; the statistics are
stored for \code{\link[=mcca_transform]{mcca_transform()}}.}
}
\value{
An object of class \code{"mcca"} with per-view \code{weights}, the stored
\code{center}/\code{scale}, view \code{dims}, \code{ncomp}, \code{reg}, kept \code{eigenvalues}, and
\code{n_views}.
}
\description{
Each view is a feature matrix on the same trials (rows aligned across views).
MCCA finds per-view linear projections whose projected scores are maximally
correlated across views. Under the SUMCOR formulation this is the generalized
eigenproblem \verb{C w = lambda B w}, where \code{C} is the full stacked covariance and
\code{B} is its block-diagonal (within-view) part with ridge regularization. It is
solved by Cholesky-whitening \code{B} (the same device used elsewhere in the
package) and taking the leading eigenvectors.
}
\examples{
set.seed(1)
s  <- rnorm(100)
V1 <- cbind(s + rnorm(100, sd = 0.1), matrix(rnorm(100 * 3), 100, 3))
V2 <- cbind(s + rnorm(100, sd = 0.1), matrix(rnorm(100 * 2), 100, 2))
mc <- mcca_train(list(V1, V2), ncomp = 2L)
consensus <- mcca_transform(mc, list(V1, V2))$consensus
}
\seealso{
\code{\link[=mcca_transform]{mcca_transform()}}
}
