% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifiers.R
\name{fisher_lda}
\alias{fisher_lda}
\title{Compute Fisher LDA direction for binary classes.}
\usage{
fisher_lda(x, label)
}
\arguments{
\item{x}{Feature matrix with shape \verb{p x n}.}

\item{label}{Binary label vector of length \code{n}.}
}
\value{
List with projection \code{direction} and normalized sample \code{score}.
}
\description{
Compute Fisher LDA direction for binary classes.
}
\examples{
set.seed(1)
X <- matrix(rnorm(6 * 40), 6, 40)        # features x samples
lab <- rep(c("a", "b"), each = 20)
fl <- fisher_lda(X, lab)
length(fl$direction)
}
