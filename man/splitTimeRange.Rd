% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Bandpass_Filtering.R, R/aliases.R
\name{splitTimeRange}
\alias{splitTimeRange}
\alias{split_time_range}
\title{Split a labelled time series into per-trial index segments.}
\usage{
splitTimeRange(labels, by_label = TRUE, minlen = NULL, seglen = NULL)

split_time_range(labels, by_label = TRUE, minlen = NULL, seglen = NULL)
}
\arguments{
\item{labels}{Integer-coercible vector of per-sample labels/markers.}

\item{by_label}{When \code{TRUE}, each maximal run of identical labels becomes one
trial. When \code{FALSE}, each run is further chopped into windows of \code{seglen}
samples, keeping only windows of at least \code{minlen} samples.}

\item{minlen}{Minimum window length for \code{by_label = FALSE}; defaults to
\code{seglen}.}

\item{seglen}{Window length in samples; required when \code{by_label = FALSE}.}
}
\value{
A list of integer index vectors, one per trial.
}
\description{
Segments a sample-indexed label/marker vector into trials, either by maximal
runs of constant label (\code{by_label = TRUE}) or into fixed-length windows
within each constant-label run (\code{by_label = FALSE}).
}
\examples{
labels <- c(rep(1, 5), rep(2, 3), rep(1, 4))
splitTimeRange(labels, by_label = TRUE)
}
