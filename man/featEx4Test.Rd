% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FeatureExtraction.R, R/aliases.R
\name{featEx4Test}
\alias{featEx4Test}
\alias{feat_ex_test}
\title{Transform test trials using a trained feature object.}
\usage{
featEx4Test(
  x,
  object,
  feature = c("logvar", "logvar_pca", "CSP", "FBCSP", "FBCSSP", "TS", "ACM_TS",
    "Riemannian", "ATM", "bandpower", "Hjorth", "MVAR", "MSVAR"),
  epsilon = 1e-06,
  skip_validation = FALSE
)

feat_ex_test(
  x,
  object,
  feature = c("logvar", "logvar_pca", "CSP", "FBCSP", "FBCSSP", "TS", "ACM_TS",
    "Riemannian", "ATM", "bandpower", "Hjorth", "MVAR", "MSVAR"),
  epsilon = 1e-06,
  skip_validation = FALSE
)
}
\arguments{
\item{x}{Trial list or single trial matrix.}

\item{object}{Train-time object returned by \code{featEx4Train}.}

\item{feature}{Feature mode, must match training branch.}

\item{epsilon}{Numerical floor used in SPD operations.}

\item{skip_validation}{When \code{TRUE}, bypasses test-time finiteness/contract
checks for use inside hot CV loops. Do not set on untrusted data.}
}
\value{
Feature matrix with one row per test trial.
}
\description{
Transform test trials using a trained feature object.
}
\examples{
set.seed(1)
x <- lapply(1:8, function(i) matrix(rnorm(128 * 4), 128, 4))
y <- factor(rep(c("L", "R"), each = 4))
fit <- featEx4Train(x, y, "logvar", list())
featEx4Test(x, fit$object, "logvar")
}
