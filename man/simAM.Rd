% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simAM.R
\name{simAM}
\alias{simAM}
\title{Re-weight EEG trials with the parameter-free SimAM attention module.}
\usage{
simAM(x, method = c("vanilla", "robust", "corr"), lambda = NULL, eps = 1e-04)
}
\arguments{
\item{x}{A numeric trial matrix (\verb{samples x channels}) or a list of such
matrices.}

\item{method}{Energy variant: \code{"vanilla"} (mean/variance), \code{"robust"}
(median/MAD), or \code{"corr"} (correlation-aware, a.k.a. Co-SimAM).}

\item{lambda}{Tuning parameter; if \code{NULL} a per-method default is used. For
\code{"robust"} it is a scale-invariant relative bandwidth on the data's robust
spread (default \code{1}; the spread auto-adapts to the data scale, so the
energy is \code{O(1)} for ordinary samples as in the vanilla variant). For
\code{"corr"} it is the cross-channel coupling strength (default \code{0.1}; \code{0}
reduces to vanilla). Ignored by \code{"vanilla"}.}

\item{eps}{Numerical-stability constant.}
}
\value{
The re-weighted input, matching the input structure (matrix or list).
}
\description{
Computes per-element attention weights from a local energy function and
multiplies the signal by \code{1 / (1 + energy)}. The transform carries no fitted
state, so train and test trials can be re-weighted independently.
}
\details{
Reference: Yang, Zhang, Li & Xie (2021), "SimAM: A Simple, Parameter-Free
Attention Module for Convolutional Neural Networks", ICML.
}
\examples{
M <- matrix(rnorm(256 * 4), 256, 4)
W <- simAM(M, method = "vanilla")
dim(W)
}
