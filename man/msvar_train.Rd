% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msvar.R
\name{msvar_train}
\alias{msvar_train}
\title{Train a per-class Markov-Switching VAR (MSVAR) generative classifier.}
\usage{
msvar_train(
  x,
  y,
  M = 2L,
  p = 1L,
  control = list(maxit = 10L, tol = 1e-05, init = "conditional"),
  seed = NULL
)
}
\arguments{
\item{x}{List of trial matrices (\verb{samples x channels}). For best results pass
low-dimensional, e.g. CSP-projected, trials.}

\item{y}{Class labels aligned with \code{x}.}

\item{M}{Number of regimes per class model.}

\item{p}{VAR order.}

\item{control}{MSVAR EM control list (\code{maxit}, \code{tol}, \code{init}, plus optional
\code{init_msvar} keys such as \code{nseg}). Defaults to a light fit; the original
pipeline used \code{maxit = 1}.}

\item{seed}{Optional integer; when supplied, \code{set.seed(seed)} is called before
the RNG-based initialization/k-means so training is reproducible.}
}
\value{
An object of class \code{"msvar"}: a list with \code{classes}, per-class
\code{models} (each a theta list), and \code{M}/\code{p}/\code{control}.
}
\description{
Fits one MSVAR (M regimes, order p) per class from that class's trials. A new
trial is later assigned (by \code{\link[=msvar_predict]{msvar_predict()}}) to the class whose model gives
the highest whole-trial log-likelihood. This reproduces the original
CSP -> MSVAR motor-imagery pipeline, with spatial filtering left to the
feature dispatcher (e.g. fit CSP/FBCSP first, then pass the projected trials).
}
\examples{
\donttest{
set.seed(1)
x <- lapply(1:12, function(i) matrix(rnorm(200 * 3), 200, 3))
y <- factor(rep(c("a", "b"), each = 6))
m <- msvar_train(x, y, M = 2L, p = 1L, seed = 1L,
                 control = list(maxit = 5L, nseg = 15L))
predict(m, x)
}
}
\references{
D. Degras, C.-M. Ting, H. Ombao (2022). Markov-switching
state-space models with applications to neuroimaging. CSDA.
}
\seealso{
\code{\link[=msvar_predict]{msvar_predict()}}
}
